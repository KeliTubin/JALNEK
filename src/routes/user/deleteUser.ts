import express from 'express';
import { router } from '../../app';
cost router = express.Router();


interface UserInput {
    id: string;
}

router.delete('/', (req, res)=> {
    try{

    } catch(error); 
    )
});